# Ф3 — Контаминация-стойкий кодинг + бейзлайн gpt-oss:20b (2026-07-04)

10 НОВЫХ compositional-задач (не туториал-канон): mutfizz(4/7-правило), vowel_ascii, digit_letter, distinct_sort,
even_digitsum, kv_parse, max_depth, safe_div, ragged_t, run_compress. **Точное имя функции обязательно** (без алиаса),
**краевые asserts** (empty/unbalanced/None-padding/zero). N=5 @ temp 0.6, think off. Обе модели, Wilson 95% CI. 0 trip/err.

## Результат

| Модель | Overall | Единственная просадка |
|---|---|---|
| **Qwen-AgentWorld-35B** | **96% [87-99] (48/50)** | run_compress 60% [23-88] |
| gpt-oss:20b (бейзлайн) | 100% [93-100] (50/50) | — |

9/10 задач — обе 100%.

## Выводы
- ✅ **КОНТАМИНАЦИЯ ОПРОВЕРГНУТА.** Qwen решает новые compositional-задачи с точным именем и краевыми на **96%** —
  это НЕ заученный туториал-код. Прямой ответ на критику Fable: кодинг-способность реальна (в отличие от насыщенного
  fizzbuzz/roman v1, который мерил память). Цифра 96% [87-99] честная, с CI.
- ⚠️ **Реальная ошибка** на run_compress (2/5): написала `result.append(s * count)` вместо `s[i-1] * count`
  (перепутала всю строку с символом) — настоящий баг переменной, не артефакт грейдера.
- **Дискриминативность низкая:** 9/10 задач оба на потолке; разделила только run_compress. Для поиска реального
  потолка Qwen vs gpt-oss нужны труднее (LiveCodeBench-easy). Но вопрос «настоящий ли кодинг» закрыт: да.
- Qwen чуть ниже gpt-oss:20b (96 vs 100), CI перекрываются → статистически не «хуже», но на этой батарее gpt-oss
  безошибочен, Qwen — почти.

## Оговорки
N=5 (CI широкие на задачу). Тесты — по 3-4 assert с краями, но не EvalPlus-полнота. Уровень сложности near-ceiling
для обеих → не находит потолок. think off только (кодинг). Один прогон gpt-oss (temp 0.6, N=5) — тоже не CI-строго на задачу.
