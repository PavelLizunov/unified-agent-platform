# hermes-agent — NousResearch hermes-agent gateway (Track A2). Brain = Codex
# (codex_app_server, owner's ChatGPT Plus OAuth) reached through the in-cluster
# singbox-egress. Official image nousresearch/hermes-agent:latest (Docker Hub —
# reachable from RU). State (config, SQLite, skills, memories, codex + hermes auth)
# lives on a local-path PVC at /opt/data. The container starts as root; the image's
# s6 /init drops services to uid 10000 (hermes). Full proven recipe + verify steps:
# runbooks/hermes-agent-codex-brain.md.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hermes-agent-data
  namespace: uap-system
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
---
# Dedicated no-permission ServiceAccount (2026-06-28 hardening): the gateway never calls the
# Kubernetes API. No RoleBinding is created, so this SA has zero RBAC; its token is not mounted either.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: hermes-agent
  namespace: uap-system
automountServiceAccountToken: false
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hermes-agent
  namespace: uap-system
spec:
  replicas: 1
  strategy:
    type: Recreate          # single-writer SQLite/auth state on one PVC — never two pods
  selector:
    matchLabels:
      app: hermes-agent
  template:
    metadata:
      labels:
        app: hermes-agent
      annotations:
        hermes-agent/config-rev: "v96-v019-upgrade"   # bump on EVERY ConfigMap/Secret change to roll the pod
    spec:
      # local-path PVC is node-local; pin to the designated always-on worker.
      nodeSelector:
        kubernetes.io/hostname: uap-home-2
      # Hardening (2026-06-28): the gateway does NOT use the Kubernetes API, so its SA token is not
      # mounted and it runs under a dedicated no-permission SA. The aggressive container hardening
      # (runAsNonRoot, capabilities.drop:[ALL]) is INTENTIONALLY NOT applied: s6-overlay (PID 1) starts
      # as root and needs CAP_SETUID/SETGID to drop the services to uid 10000, and the Codex worker runs
      # sandbox_mode=danger-full-access. seccompProfile RuntimeDefault is the compatible tightening; the
      # rest is the documented, accepted pod boundary. (allowPrivilegeEscalation:false is a safe future
      # add -- it blocks gaining privs, not the setuid() drop -- pending a check for any suid helper.)
      automountServiceAccountToken: false
      serviceAccountName: hermes-agent
      securityContext:
        fsGroup: 10000        # PVC group-owned by hermes so the uid-10000 services can write
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        - name: bootstrap
          image: nousresearch/hermes-agent@sha256:f7b35053268f532f98955195c909f15a230470fbcbdacaa9fdecb95707dad04a  # v0.19.0 (Docker tag v2026.7.20), pinned by digest 2026-07-24
          env:
            - { name: HTTPS_PROXY, value: "http://singbox-egress-ha.uap-system.svc:12080" }
            - { name: HTTP_PROXY, value: "http://singbox-egress-ha.uap-system.svc:12080" }
            - { name: NO_PROXY, value: ".svc,.cluster.local,localhost,127.0.0.1,10.0.0.0/8,100.64.0.0/10" }
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -eu
              # 1) coding CLIs onto the PVC (land in /opt/data/.local/bin, already on the image PATH)
              export npm_config_prefix=/opt/data/.local
              # CLI versions PINNED (was unpinned): a fresh PVC re-installs the exact tested versions,
              # so the deployed worker matches the one the self-test gate validated. Seed-if-absent, so
              # the running PVC is untouched; bump these deliberately.
              if [ ! -x /opt/data/.local/bin/codex ]; then
                npm i -g @openai/codex@0.142.0
              fi
              if [ ! -x /opt/data/.local/bin/claude ]; then
                npm i -g @anthropic-ai/claude-code@2.1.193
              fi
              # Local voice-note STT: cache only checksum-pinned artifacts on the PVC, then
              # unpack the tiny runtime into this pod's emptyDir. Audio never enters this cache.
              stt_cache=/opt/data/.cache/uap-stt
              mkdir -p "$stt_cache" /stt-runtime/python /stt-runtime/native
              fetch_stt_asset() {
                stt_url=$1
                stt_sha256=$2
                stt_destination=$3
                if [ -f "$stt_destination" ] && printf '%s  %s\n' "$stt_sha256" "$stt_destination" | sha256sum -c - >/dev/null 2>&1; then
                  return
                fi
                stt_partial="${stt_destination}.part"
                rm -f "$stt_partial"
                curl --fail --location --silent --show-error --retry 3 \
                  --connect-timeout 15 --max-time 300 --output "$stt_partial" "$stt_url"
                printf '%s  %s\n' "$stt_sha256" "$stt_partial" | sha256sum -c - >/dev/null
                mv "$stt_partial" "$stt_destination"
              }
              fetch_stt_asset \
                https://github.com/handy-computer/transcribe.cpp/releases/download/v0.1.3/transcribe_cpp-0.1.3-py3-none-any.whl \
                736f366beb8093eebd1a2ea694de48b6f87a34c6e5eb332384ba96fe3f4fceb3 \
                "$stt_cache/transcribe_cpp-0.1.3.whl"
              fetch_stt_asset \
                https://github.com/handy-computer/transcribe.cpp/releases/download/v0.1.3/transcribe-native-0.1.3-linux-x86_64-cpu-vulkan.tar.gz \
                5e150c7862748d33dc2f559a38274bcb46d06ba63f8f5d1247f8196569e02797 \
                "$stt_cache/transcribe-native-0.1.3-linux-x86_64-cpu-vulkan.tar.gz"
              fetch_stt_asset \
                'https://huggingface.co/handy-computer/gigaam-v3-e2e-rnnt-gguf/resolve/f719d70812344f4d0fb8c11c0887b190501a7465/gigaam-v3-e2e-rnnt-Q4_K_M.gguf?download=true' \
                7d69952fb431a8d7800ed9910dc61fea37d8406bfe96d10bf24c8bd4b7c68623 \
                "$stt_cache/gigaam-v3-e2e-rnnt-Q4_K_M.gguf"
              /opt/hermes/.venv/bin/python -m zipfile -e \
                "$stt_cache/transcribe_cpp-0.1.3.whl" /stt-runtime/python
              tar -xzf "$stt_cache/transcribe-native-0.1.3-linux-x86_64-cpu-vulkan.tar.gz" \
                -C /stt-runtime/native --strip-components=1
              cp /mission-overlay/local_stt.py /stt-runtime/local_stt.py
              chmod 0555 /stt-runtime/local_stt.py
              # 2) Soft config + codex sandbox config from the ConfigMap, SEED-IF-ABSENT so the
              #    dashboard/CLI can own them on the PVC across restarts. The BRAIN is pinned
              #    separately by the managed overlay /etc/hermes/config.yaml, which always wins.
              [ -f /opt/data/config.yaml ] || cp /config/config.yaml /opt/data/config.yaml
              mkdir -p /opt/data/.codex
              [ -f /opt/data/.codex/config.toml ] || cp /config/codex-config.toml /opt/data/.codex/config.toml
              mkdir -p /opt/data/mcp
              # MCP policy is GitOps-owned; refresh it every roll so new bounded tools cannot be shadowed
              # by the seed-only copy left on the PVC by an older revision.
              cp /config/build1-mcp /opt/data/mcp/build1_mcp.py
              # ornith coder wrapper. Copy ALWAYS (not seed-if-absent) so a ConfigMap edit propagates on the next roll.
              mkdir -p /opt/data/.local/bin
              cp /config/ornith-wrapper /opt/data/.local/bin/ornith && chmod +x /opt/data/.local/bin/ornith
              # Codex AGENTS.md is managed runtime policy, not user memory: refresh it every roll.
              cp /config/agents-md /opt/data/.codex/AGENTS.md
              # 2a) claude -p worker: seed deny-first settings (defense-in-depth vs accidental secret reads;
              #     Bash is unconstrained, so the pod itself is the real boundary)
              mkdir -p /opt/data/.claude
              [ -f /opt/data/.claude/settings.json ] || cp /config/claude-settings /opt/data/.claude/settings.json
              # 2b) Fail CLOSED on a broken brain overlay: the brain lives ONLY in the managed
              #     overlay now (no PVC fallback on a fresh volume), and the managed loader is
              #     fail-OPEN, so a malformed overlay would silently un-pin the brain. Abort the boot
              #     (visible CrashLoop) instead. /config/managed-config = the bytes the gateway
              #     mounts at /etc/hermes/config.yaml.
              /opt/hermes/.venv/bin/python -c 'import yaml,sys; d=yaml.safe_load(open("/config/managed-config")) or {}; m=d.get("model",{}); sys.exit("FATAL: managed brain overlay missing model.provider/default") if not all(m.get(k) for k in ("provider","default")) else None'
              # 2c) ADR-027: patch exact known defects in the pinned upstream v0.19 sources.
              # Work on emptyDir copies; the official image stays immutable. Unknown source = fail closed.
              cp /opt/hermes/agent/display.py /runtime-compat/display.py
              cp /opt/hermes/agent/tool_guardrails.py /runtime-compat/tool_guardrails.py
              cp /opt/hermes/agent/codex_runtime.py /runtime-compat/codex_runtime.py
              cp /opt/hermes/agent/transports/codex_event_projector.py /runtime-compat/codex_event_projector.py
              cp /opt/hermes/cli.py /runtime-compat/cli.py
              cp /opt/hermes/hermes_cli/web_dist/assets/index-DE8KfxAS.js /runtime-compat/web_index.js
              /opt/hermes/.venv/bin/python /config/runtime-compat.py /runtime-compat
              # 2d) ADR-030: stage the exact pinned mission overlay in a second emptyDir.
              # The overlay fingerprints upstream before modifying it and fails closed on drift.
              mkdir -p /mission-runtime/root/hermes_cli /mission-runtime/root/gateway/platforms /mission-runtime/root/plugins/platforms/telegram
              cp /opt/hermes/hermes_cli/commands.py /mission-runtime/root/hermes_cli/commands.py
              cp /opt/hermes/hermes_cli/kanban.py /mission-runtime/root/hermes_cli/kanban.py
              cp /opt/hermes/hermes_cli/kanban_db.py /mission-runtime/root/hermes_cli/kanban_db.py
              cp /opt/hermes/hermes_cli/main.py /mission-runtime/root/hermes_cli/main.py
              cp /opt/hermes/gateway/run.py /mission-runtime/root/gateway/run.py
              cp /opt/hermes/gateway/platforms/api_server.py /mission-runtime/root/gateway/platforms/api_server.py
              cp /opt/hermes/plugins/platforms/telegram/adapter.py /mission-runtime/root/plugins/platforms/telegram/adapter.py
              /opt/hermes/.venv/bin/python /mission-overlay/apply_overlay.py /mission-runtime/root \
                --source-commit 3ef6bbd201263d354fd83ec55b3c306ded2eb72a
              cp /mission-runtime/root/hermes_cli/commands.py /mission-runtime/commands.py
              cp /mission-runtime/root/hermes_cli/kanban.py /mission-runtime/kanban.py
              cp /mission-runtime/root/hermes_cli/kanban_db.py /mission-runtime/kanban_db.py
              cp /mission-runtime/root/hermes_cli/main.py /mission-runtime/main.py
              cp /mission-runtime/root/hermes_cli/uap_missions.py /mission-runtime/uap_missions.py
              cp /mission-runtime/root/hermes_cli/uap_media.py /mission-runtime/uap_media.py
              cp /mission-runtime/root/gateway/run.py /mission-runtime/gateway_run.py
              cp /mission-runtime/root/gateway/platforms/api_server.py /mission-runtime/api_server.py
              cp /mission-runtime/root/plugins/platforms/telegram/adapter.py /mission-runtime/telegram_adapter.py
              # 3) seed Codex creds ONCE — after first boot hermes refreshes its own
              #    /opt/data/auth.json, so never clobber it (single-use refresh tokens).
              if [ ! -f /opt/data/auth.json ]; then
                cp /seed/auth.json /opt/data/.codex/auth.json
                HOME=/opt/data CODEX_HOME=/opt/data/.codex /opt/hermes/.venv/bin/python -c \
                  'import sys; from hermes_cli.auth import _import_codex_cli_tokens, _save_codex_tokens; t=_import_codex_cli_tokens(); sys.exit("codex token import failed (expired/invalid seed?)") if not t else _save_codex_tokens(t)'
              fi
              # 3b) Telegram bot token -> user .env (hermes reads it as self.config.token).
              #     Proxy + allowlist are NOT here — they go in the managed scope
              #     (/etc/hermes/.env) so they reach the isolated per-profile os.environ.
              # The bot token is GitOps-owned (seeded from the SOPS secret, ROTATABLE by rolling
              # the pod). Replace ONLY its line each boot; PRESERVE all OTHER .env lines the
              # dashboard writes (Discord/MCP tokens, /sethome chat-IDs) so they survive restarts.
              touch /opt/data/.env
              grep -v '^TELEGRAM_BOT_TOKEN=' /opt/data/.env > /opt/data/.env.tmp || true
              printf 'TELEGRAM_BOT_TOKEN=%s\n' "$(cat /telegram/token)" >> /opt/data/.env.tmp
              mv /opt/data/.env.tmp /opt/data/.env
              chmod 600 /opt/data/.env
              # 3c) seed the user profile (memories/USER.md) once; the agent enriches it via chat
              mkdir -p /opt/data/memories
              [ -f /opt/data/memories/USER.md ] || cp /config/user-profile /opt/data/memories/USER.md
              # Preserve enriched owner memory, but remove the known 2026-07-06 model/coder drift.
              /opt/hermes/.venv/bin/python /config/profile-migrate.py
              # 4a) durable ssh for the AGENT: root-owned /root/.ssh (shared emptyDir with the gateway)
              #     so bare `ssh build-1` from the agent bash -lc survives pod rolls (the ephemeral
              #     in-pod fix kept getting wiped). config resolves the build-1 alias; key stays on PVC.
              mkdir -p /root/.ssh; chmod 700 /root/.ssh
              [ -f /opt/data/.ssh/config ] && cp /opt/data/.ssh/config /root/.ssh/config && chmod 600 /root/.ssh/config || true
              [ -f /opt/data/.ssh/known_hosts ] && cp /opt/data/.ssh/known_hosts /root/.ssh/known_hosts || true
              chown -R 0:0 /root/.ssh
              # 4) ownership for the uid-10000 hermes services
              chown -R 10000:10000 /opt/data
          volumeMounts:
            - { name: data, mountPath: /opt/data }
            - { name: config, mountPath: /config, readOnly: true }
            - { name: codex-seed, mountPath: /seed, readOnly: true }
            - { name: telegram-seed, mountPath: /telegram, readOnly: true }
            - { name: root-ssh, mountPath: /root/.ssh }
            - { name: runtime-compat, mountPath: /runtime-compat }
            - { name: mission-overlay, mountPath: /mission-overlay, readOnly: true }
            - { name: mission-runtime, mountPath: /mission-runtime }
            - { name: stt-runtime, mountPath: /stt-runtime }
      containers:
        - name: gateway
          image: nousresearch/hermes-agent@sha256:f7b35053268f532f98955195c909f15a230470fbcbdacaa9fdecb95707dad04a  # v0.19.0 (Docker tag v2026.7.20), pinned by digest 2026-07-24
          args: ["gateway", "run"]
          env:
            - { name: HERMES_HOME, value: /opt/data }
            - { name: CODEX_HOME, value: /opt/data/.codex }
            # Codex CLI egress -> singbox-egress-ha (urltest failover) -> VLESS exit -> chatgpt.com (the proven path)
            - { name: HTTPS_PROXY, value: "http://singbox-egress-ha.uap-system.svc:12080" }
            - { name: HTTP_PROXY, value: "http://singbox-egress-ha.uap-system.svc:12080" }
            - { name: NO_PROXY, value: ".svc,.cluster.local,localhost,127.0.0.1,10.0.0.0/8,100.64.0.0/10" }
            - { name: HERMES_DISABLE_LAZY_INSTALLS, value: "1" }
            # Web dashboard (the "morda": config + sessions UI), over tailnet via the NodePort Service below.
            # Auth is mandatory (June 2026 hardening); basic auth, password from the SOPS secret.
            - { name: HERMES_DASHBOARD, value: "1" }
            - { name: HERMES_DASHBOARD_HOST, value: "0.0.0.0" }
            - { name: HERMES_DASHBOARD_PORT, value: "9119" }
            - { name: HERMES_DASHBOARD_BASIC_AUTH_USERNAME, value: "pavel" }
            - name: HERMES_DASHBOARD_BASIC_AUTH_PASSWORD
              valueFrom: { secretKeyRef: { name: hermes-agent-dashboard, key: password } }
            # A4 - Claude Max OAuth token for the claude -p coding worker (claude reads CLAUDE_CODE_OAUTH_TOKEN)
            - name: CLAUDE_CODE_OAUTH_TOKEN
              valueFrom: { secretKeyRef: { name: hermes-agent-claude, key: oauth-token } }
            - { name: API_SERVER_ENABLED, value: "true" }
            - { name: API_SERVER_HOST, value: "0.0.0.0" }
            - { name: API_SERVER_PORT, value: "8642" }
            - name: API_SERVER_KEY
              valueFrom: { secretKeyRef: { name: hermes-agent-api, key: api-key } }
            - name: HERMES_MISSION_PRODUCER_KEY
              valueFrom: { secretKeyRef: { name: hermes-agent-mission, key: producer-key } }
            - name: HERMES_MISSION_OWNER_KEY
              valueFrom: { secretKeyRef: { name: hermes-agent-owner, key: owner-key } }
            - { name: HERMES_OWNER_COMMANDS, value: "projects,mission,status,help,stop" }
            - name: HERMES_MISSION_PROJECTS
              valueFrom:
                configMapKeyRef:
                  name: hermes-project-catalog
                  key: projects.json
            # Keep ordinary Telegram conversation in the two UAP-owned topics.
            # Other group topics require an explicit bot mention or reply.
            - { name: TELEGRAM_REQUIRE_MENTION, value: "true" }
            - { name: TELEGRAM_FREE_RESPONSE_TOPICS, value: "-1004377555987:2,-1004377555987:3" }
            # Route unattended Hermes notices and cron results to Code.
            - { name: TELEGRAM_HOME_CHANNEL, value: "-1004377555987" }
            - { name: TELEGRAM_HOME_CHANNEL_THREAD_ID, value: "2" }
            # Telegram forum-topic allowlist for media routing.  JSON array of
            # exact {"chat_id","thread_id"} pair objects whose ordinary text/voice
            # deterministically enters media.image.generate instead of project
            # intake.  Telegram thread IDs are chat-local, so both fields are
            # required.  Empty = disabled (fail-closed).  Owner sets real pair
            # after creating the supergroup; do NOT hardcode production IDs here.
            - { name: HERMES_MISSION_MEDIA_TOPICS, value: '[{"chat_id":"-1004377555987","thread_id":"3"}]' }
          ports:
            - { name: dashboard, containerPort: 9119 }
            - { name: api, containerPort: 8642 }
          # Probes (2026-06-28): a TCP connect to the dashboard port proves the gateway is actually
          # listening (Ready != merely "process running"). The dashboard requires basic auth, so an
          # HTTP GET would 401 -- TCP is the safe, auth-free signal. startupProbe gives the gateway's
          # own start ~5 min before liveness engages. (The initContainer's CLI install runs to
          # completion BEFORE this container starts and is not gated by these probes.)
          startupProbe:
            tcpSocket: { port: 9119 }
            periodSeconds: 10
            failureThreshold: 30      # up to ~5 min for the gateway to bind 9119
          readinessProbe:
            tcpSocket: { port: 9119 }
            periodSeconds: 15
            failureThreshold: 3
          livenessProbe:
            tcpSocket: { port: 9119 }
            periodSeconds: 20
            failureThreshold: 5       # ~100s before restarting a single-replica Recreate agent (margin for a transient listener blip)
          volumeMounts:
            - { name: data, mountPath: /opt/data }
            # F4 / upstream #62336: if any skill ever invokes hermes's LOCAL terminal tool, its snapshot
            # path (cache/terminal/hermes-snap-*.sh, HERMES_HOME-relative) would capture the gateway's full
            # secret env in PLAINTEXT onto the durable PVC -> the daily R2 backup. Shadow that DISPOSABLE
            # cache dir with an emptyDir so any snapshot is ephemeral (gone on roll, never backed up).
            # Currently inert: the Codex loop, not hermes's terminal tool, drives execution (live find = 0
            # snapshots). Path is per upstream tools/environments/base.py; confirm via the canary in
            # SECURITY-REMEDIATION-2026-07-24.md before relying on it if the local terminal tool is enabled.
            - { name: terminal-cache, mountPath: /opt/data/cache/terminal }
            - { name: research-session, mountPath: /opt/data/mcp/research_session.py, subPath: research_session.py, readOnly: true }
            # managed scope: loaded LAST with override into os.environ, authoritative for
            # every profile + subprocess (codex spawn = os.environ.copy()). This is how the
            # Telegram adapter AND the gateway-driven codex brain reach the egress proxy —
            # the per-profile os.environ is isolated and does NOT see /opt/data/.env.
            - { name: root-ssh, mountPath: /root/.ssh }
            - { name: config, mountPath: /etc/hermes/.env, subPath: managed-env, readOnly: true }
            # managed CONFIG overlay: pins the BRAIN (model.*) per-leaf OVER the PVC config.yaml.
            - { name: config, mountPath: /etc/hermes/config.yaml, subPath: managed-config, readOnly: true }
            # ADR-027 temporary overlays for the pinned upstream digest; bootstrap validates exact fingerprints.
            - { name: runtime-compat, mountPath: /opt/hermes/agent/display.py, subPath: display.py, readOnly: true }
            - { name: runtime-compat, mountPath: /opt/hermes/agent/tool_guardrails.py, subPath: tool_guardrails.py, readOnly: true }
            - { name: runtime-compat, mountPath: /opt/hermes/agent/codex_runtime.py, subPath: codex_runtime.py, readOnly: true }
            - { name: runtime-compat, mountPath: /opt/hermes/agent/transports/codex_event_projector.py, subPath: codex_event_projector.py, readOnly: true }
            - { name: runtime-compat, mountPath: /opt/hermes/cli.py, subPath: cli.py, readOnly: true }
            - { name: runtime-compat, mountPath: /opt/hermes/hermes_cli/web_dist/assets/index-DE8KfxAS.js, subPath: web_index.js, readOnly: true }
            # ADR-030 central mission runtime, generated from the exact A6.3 overlay sources.
            - { name: mission-runtime, mountPath: /opt/hermes/hermes_cli/commands.py, subPath: commands.py, readOnly: true }
            - { name: mission-runtime, mountPath: /opt/hermes/hermes_cli/kanban.py, subPath: kanban.py, readOnly: true }
            - { name: mission-runtime, mountPath: /opt/hermes/hermes_cli/kanban_db.py, subPath: kanban_db.py, readOnly: true }
            - { name: mission-runtime, mountPath: /opt/hermes/hermes_cli/main.py, subPath: main.py, readOnly: true }
            - { name: mission-runtime, mountPath: /opt/hermes/hermes_cli/uap_missions.py, subPath: uap_missions.py, readOnly: true }
            - { name: mission-runtime, mountPath: /opt/hermes/hermes_cli/uap_media.py, subPath: uap_media.py, readOnly: true }
            - { name: mission-runtime, mountPath: /opt/hermes/gateway/run.py, subPath: gateway_run.py, readOnly: true }
            - { name: mission-runtime, mountPath: /opt/hermes/gateway/platforms/api_server.py, subPath: api_server.py, readOnly: true }
            - { name: mission-runtime, mountPath: /opt/hermes/plugins/platforms/telegram/adapter.py, subPath: telegram_adapter.py, readOnly: true }
            - { name: stt-runtime, mountPath: /opt/uap-stt, readOnly: true }
          resources:
            # home-2 resized to 6c/8GB (2026-07). Bumped so a stray heavy task (browser/build) in the pod
            # fits (agent ~1.1GB + chromium ~2GB) instead of OOM-killing the gateway. Norm is still heavy->build-1.
            requests: { cpu: 250m, memory: 1Gi }
            limits: { memory: 4Gi }
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: hermes-agent-data
        # F4 / #62336: ephemeral terminal-snapshot cache shadowing /opt/data/cache/terminal (see the
        # gateway volumeMount note). A future local-terminal snapshot lands here, NOT on the PVC/backup.
        - name: terminal-cache
          emptyDir: {}
        - name: config
          configMap:
            name: hermes-agent-config
        - name: codex-seed
          secret:
            secretName: codex-auth
        - name: telegram-seed
          secret:
            secretName: hermes-agent-telegram
        - name: root-ssh
          emptyDir: {}
        - name: runtime-compat
          emptyDir: {}
        - name: mission-runtime
          emptyDir: {}
        - name: stt-runtime
          emptyDir: {}
        - name: mission-overlay
          configMap:
            name: hermes-mission-runtime
        - name: research-session
          configMap:
            name: hermes-research-session
---
# Web dashboard. The cluster network runs on tailscale0 (ADR-021), so a tailscale-serve proxy can't
# reach a ClusterIP — use a NodePort (same pattern as Hermes-legacy :30890). NOTE: a k3s NodePort binds
# 0.0.0.0 on EVERY node interface, so :30911 is reachable on the tailnet AND the home LAN (192.168.0.0/24);
# there is NO network-layer restriction yet (no NetworkPolicy — proposed in
# SECURITY-REMEDIATION-2026-07-24.md). Access is controlled by mandatory basic auth (user "pavel"),
# e.g. http://<any-node-tailnet-ip>:30911. It is NOT tailnet-only.
apiVersion: v1
kind: Service
metadata:
  name: hermes-agent-dashboard
  namespace: uap-system
spec:
  type: NodePort
  selector:
    app: hermes-agent
  ports:
    - name: dashboard
      port: 9119
      targetPort: 9119
      nodePort: 30911
---
apiVersion: v1
kind: Service
metadata:
  name: hermes-agent-api
  namespace: uap-system
spec:
  type: NodePort
  selector:
    app: hermes-agent
  ports:
    - name: api
      port: 8642
      targetPort: 8642
      nodePort: 30642
