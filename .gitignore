# Local secrets and generated credentials
.env
.env.*
*.key
*.pem
*.p12
*.pfx
*.kubeconfig
kubeconfig*

# SOPS/age private keys must stay outside the repo
age-key.txt
keys.txt

# Local tooling noise
.DS_Store
Thumbs.db
*.log
tmp/
.tmp/

# OpenTofu / Terraform local state and private inputs
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ansible local noise and private vault helpers
*.retry
.vault-pass
vault-password.txt
